{
  "name": "@tools/image",
  "private": true,
  "type": "module",
  "scripts": {
    "build:vendor": "node scripts/vendor.mjs",
    "build:css": "tailwindcss -i src/styles.css -o public/styles.css --minify",
    "build:js": "esbuild src/client/app.ts --bundle --minify --format=esm --splitting --outdir=public/js",
    "build": "pnpm build:vendor && pnpm build:css && pnpm build:js",
    "dev": "pnpm build && wrangler dev",
    "deploy": "pnpm build && wrangler deploy",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.client.json --noEmit",
    "check": "node --test"
  },
  "dependencies": {
    "@claudetools/seo": "workspace:*",
    "hono": "^4.8.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250712.0",
    "@fontsource/bricolage-grotesque": "^5.2.0",
    "@fontsource/instrument-sans": "^5.2.0",
    "@tailwindcss/cli": "^4.1.0",
    "alpinejs": "^3.14.0",
    "esbuild": "^0.28.0",
    "fflate": "^0.8.2",
    "heic-to": "^1.1.0",
    "onnxruntime-web": "^1.27.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.9.0",
    "wrangler": "^4.26.0"
  }
}
