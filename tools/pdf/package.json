{
  "name": "@tools/pdf",
  "private": true,
  "type": "module",
  "scripts": {
    "build:vendor": "node scripts/vendor.mjs",
    "build:css": "tailwindcss -i src/styles.css -o public/styles.css --minify",
    "build:js": "rm -rf public/js && esbuild src/client/app.ts --bundle --minify --format=esm --splitting --outdir=public/js",
    "build": "pnpm build:vendor && pnpm build:css && pnpm build:js",
    "dev": "pnpm build && wrangler dev",
    "deploy": "pnpm build && wrangler deploy",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.client.json --noEmit",
    "check": "node --test"
  },
  "dependencies": {
    "hono": "^4.8.0",
    "@claudetools/seo": "workspace:*"
  },
  "devDependencies": {
    "@cantoo/pdf-lib": "^2.5.0",
    "@cloudflare/workers-types": "^4.20250712.0",
    "@fontsource/caveat": "^5.2.0",
    "@fontsource/fraunces": "^5.2.0",
    "@fontsource/public-sans": "^5.2.0",
    "@tailwindcss/cli": "^4.1.0",
    "alpinejs": "^3.14.0",
    "esbuild": "^0.28.0",
    "fflate": "^0.8.2",
    "pdfjs-dist": "^4.10.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.9.0",
    "wrangler": "^4.26.0"
  }
}
